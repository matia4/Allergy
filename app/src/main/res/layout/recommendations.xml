<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/safe_alternatives"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_marginBottom="12dp" />

    <!-- Loading spinner shown during API search -->
    <ProgressBar
        android:id="@+id/pbLoadingRecs"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="visible"
        android:layout_margin="20dp" />

    <!-- Error message shown when no safe alternatives are found -->
    <TextView
        android:id="@+id/tvNoRecs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/couldnt_find"
        android:textAlignment="center"
        android:textColor="#757575"
        android:visibility="gone"
        android:layout_margin="20dp" />

    <!-- List of recommended safe alternatives -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvRecommendations"
        android:layout_width="match_parent"
        android:layout_height="350dp"
        android:visibility="gone" />

    <!-- Displays current search level/category in the hierarchy -->
    <TextView
        android:id="@+id/tvCurrentCategoryDisplay"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/category"
        android:textSize="12sp"
        android:textColor="@android:color/darker_gray"
        android:layout_marginBottom="8dp"
        android:visibility="gone" />

</LinearLayout>